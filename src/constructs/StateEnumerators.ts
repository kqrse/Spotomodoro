export enum WorkState {
    Work,
    Break,
}

export enum PauseState {
    Paused,
    Unpaused
}

export enum SettingsState {
    Displayed,
    Hidden
}

export enum TimerInitialized {
    Initialized,
    Uninitialized
}

export enum LoadState {
    Loading,
    Ready
}